name: worktime_widgetbook
description: Widgetbook for the Worktime UI prototype.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  widgetbook: ^3.25.0
  shadcn_ui: ^0.55.0
  worktime:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
